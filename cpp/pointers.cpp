// notes
/*
int temp = 10;
// &temp -> gets the adress
int* pointer = &temp; // takes a address
// pointer -> holds the address
// *pointer -> dereferance(gets the value in address)
// int &temp = 10;// alias -> referance variable holds the actual value
// temp -> get the actual value
// &temp -> address
*/

/*
// pass by pointer
int in(int *value){
   (*value)++;
    return (*value);
}

// pass by reference
int in(int value){
    value ++;
    cout << "in->" << &value << endl;
    return value;
}
*/

// struct and the class are same but 
// struct (var and funtions) are by default public but
// the class is by deafult private 
