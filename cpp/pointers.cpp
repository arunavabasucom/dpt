#include <stdc++.h>
#include <arr.h>
#include <ll.h>

using namespace std;


int main(){
    vector<int> arr = {1, 2, 3, 5};
    // ListNode *head = __llarrayToList(arr);
    // __llprint(head);
    // // cout << "hello";
    __veprint(arr);
    return 0;
}